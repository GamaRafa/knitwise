import { IProjectRepository } from "@/domain/project/IProjectRepository";
import { Project } from "@/domain/project/Project";

export function createProject(repository: IProjectRepository, name: string): Promise<Project> {
  
}